# TODO

## 下一步建議
- 增加稽核頁面（Audit Log）查詢與篩選 UI（目前僅提供 API）。
- 補上前端端對端測試（Playwright），覆蓋拖放、匯入預覽、快照回復。
- 支援 JWT / OAuth2，取代目前 Header 金鑰模式。
- 備份保留政策（例如僅保留最近 N 份，或按天/週清理）。
- 匯入差異視覺化（列出將新增/刪除/更新的節點明細）。
- 支援觸控裝置拖放手勢優化（行動裝置）。
- 增加「API 位址 + 權限設定」引導流程（首次使用導覽）。
