# TODO

## 下一步建議
- 補上自動化測試專案（xUnit），覆蓋 `OrgChartService` 的核心行為。
- 增加節點搜尋/篩選功能（依部門、人員、代碼）。
- 支援拖放（drag-and-drop）調整節點層級與順序。
- 新增角色與權限（唯讀/編輯）。
- 匯入前加入格式檢核與預覽模式，避免誤覆蓋。
- 增加資料備份與版本快照機制。
- 在前端提供可視化 API 位址設定面板（避免僅靠 URL 參數）。
