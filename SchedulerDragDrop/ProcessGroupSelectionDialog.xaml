<Window x:Class="SchedulerDragDrop.ProcessGroupSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="選擇 PRDOPGP 群組"
        Height="520"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="可多選群組，按確定後套用到查詢條件。" Foreground="#374151" Margin="0,0,0,8" />

        <Border Grid.Row="1" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="6" Background="#FFFFFF">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Options}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="4,3" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center" Foreground="#4B5563" Text="提示：不勾選代表全部群組" />
            <Button Grid.Column="1" Width="72" Height="32" Margin="8,0,0,0" Click="SelectAll_Click">全選</Button>
            <Button Grid.Column="2" Width="72" Height="32" Margin="8,0,0,0" Click="ClearAll_Click">清除</Button>
            <Button Grid.Column="3" Width="72" Height="32" Margin="16,0,0,0" IsDefault="True" Click="Ok_Click">確定</Button>
            <Button Grid.Column="4" Width="72" Height="32" Margin="8,0,0,0" IsCancel="True">取消</Button>
        </Grid>
    </Grid>
</Window>
