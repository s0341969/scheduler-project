# TODO

- 將 `appsettings.json` 的資料庫帳密改為環境變數或使用者祕密，避免明文設定檔。
- 為 `/api/drawing-spec/pdf-file` 增加允許路徑白名單，避免被濫用成任意檔案讀取入口。
- 將前端 `index.html` 中的大型腳本拆分成可維護的模組。
- 為規格 API 與版本儲存 API 補上整合測試。
- 規劃將圖面檔改為檔案儲存或物件儲存，而不是長期保存在資料庫 Base64 欄位。
- 若第二個結果集未來增加排序欄位，可讓下拉選單依建立日或來源做分組顯示。
- 若同一筆圖號/版次/規格會回多個同名 PDF，需補唯一識別資訊避免下拉選單重名難辨識。
- 若第二步驟欄位內容再增加，需評估在小螢幕下是否改成分段摺疊，避免欄位過長。
- 若使用者仍覺得流程不直覺，可進一步把 `.json` 模板載入從第 1 步移出，避免和規格查詢混在同欄。
- 若第 2 步再加入更多控制項，需重新評估 220px 窄欄是否足夠，避免按鈕或下拉被擠壓。
- 若後續常用情境仍以本機上傳為主，可考慮記住上次展開狀態，避免每次都要手動展開挑選檔案。
- 若後續仍覺得泡泡太大，可再細分為一般顯示尺寸與高縮放作業尺寸兩套樣式。
- 若未來要支援鍵盤快速定位，可補上輸入項次後自動高亮右側 Grid 與圖面泡泡的快捷流程。
- 若之後部署到不同主機，需同步檢查 `appsettings.json` 的 `Hosting:Url` 是否仍符合該機器網卡設定。
- 若未來切換到 IIS 正式部署，需提醒使用者 `Hosting:Url` 不再是唯一來源，IIS Binding 會優先決定外部入口位址。
