# TODO

- 補上整合測試，驗證樣本 PDF 的實際圓圈偵測結果與輸出 JSON/CSV。
- 增加 CLI 例外處理與非 0 結束碼，讓批次執行失敗時更容易診斷。
- 評估加入範本比對與 OCR 的效能量測，建立不同 DPI 的建議參數。
- 讓合法數字範圍過濾更早介入候選篩選，避免目前「辨識後才過濾」仍承受完整 OCR 成本。
- 針對 `1` 到 `100` 模板庫補上多字型、多粗細、多掃描雜訊版本，降低單一模板風格偏差。
- 評估將 `--fast-mode` 擴充成分階段輸出，額外匯出「僅找到圓圈」的中間結果供人工複核。
- 規劃預覽圖標記色彩與文字樣式可參數化，支援不同圖面背景與輸出需求。
- 評估為 `run_pdf_circle_locator_fast.bat` 補上可自訂 `FAST_PROBE_DPI` 與預覽輸出的版本。
- 若未來需要中文批次介面，評估改用 `.ps1` 啟動腳本取代 `.bat`，降低 Windows 碼頁相容性問題。
