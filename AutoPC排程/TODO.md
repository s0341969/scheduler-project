# TODO

## High Priority

- 機台基本資料來源設定化：支援指定固定來源（例如 `SFC3138NET_機台狀態` / 其他主檔），避免僅依欄位規則偵測。
- 以正式 DB 結構確認 `機台基本資料`、`指派時間` 的中文欄位名稱，移除目前的欄位偵測邏輯並改為固定欄位。
- 將 `SOPNAME` 納入排程約束（製程序、前後站關係）。
- 補齊「可排程工作前置完成條件」的明確過濾條件（目前僅以可排工時 > 0 篩選）。

## Medium Priority

- 新增排程衝突檢查報表（機台超載、交期落後、未排入原因統計）。
- 新增單元測試：工作優先序、機台候選選擇、跨天切段。
- 將演算法參數化（開工時間、搜尋天數、是否允許拆單）。
- GridView 匯出（CSV/Excel）功能。
- 既有指派資料量大時，補上 DB 端索引/分段載入策略（避免 `指派時間` 全表掃描）。

## Low Priority

- 支援重排模式（先保留既有排程，僅插入新工單）。
- 增加 CLI 參數驗證與錯誤訊息細化。
- 表單模式加入取消排程按鈕。

## Completed

- [x] 新增 WinForms 表單模式，改為按「開始排程」才執行（2026-03-15）。
- [x] 新增 GridView 顯示新排程清單，並加入未排入原因列表（2026-03-15）。
- [x] 新增排程進度顯示（進度條、已排/未排統計、目前工單）（2026-03-15）。
- [x] 新增待排工作總數顯示（2026-03-15）。
- [x] 新增畫面查詢欄位：機台、圖號、製卡、製程（代碼/名稱）（2026-03-15）。
- [x] GridView 新增製程名稱欄位（2026-03-15）。
- [x] 新增畫面排程方式說明（2026-03-15）。
- [x] 進度文字改為顯示製卡 / 製程 / 圖號（2026-03-15）。
- [x] 排程機台改為依 `WORKFIXM`（`MTYPE='1'`；先 `INDWG`、再 `PRDNAME`）選擇：`MAHNO` 指定機台，否則 `MAHNO_GP` 取組內最小負荷機台（2026-03-15）。
- [x] 每日標準工時改由 `機台基本資料` 載入（2026-03-15）。
- [x] 機台起始時間改為參考 `指派時間` 最後 `EndTime`，無資料才從 08:00（2026-03-15）。
- [x] 表單開始排程流程優化（先刷新畫面 + 進度更新節流），降低卡頓（2026-03-15）。
- [x] 新增工時單位自動正規化（分鐘轉小時）：套用 每日標準工時 / WKTIME / 優先工時（2026-03-15）。

