# 對話紀錄（2026-04-07）

## 目標
- 持續優化 `dbo.產生ORDE3剩餘製程` 效能。
- 針對 `%` 全量執行做分段量測，定位瓶頸並迭代修正。
- 協助部署 TEST、跑 benchmark、同步文件、推送 Git。

## 主要過程
1. 使用者要求持續優化與部署，並多次實測 `%` 全量。
2. 針對 CMM 區段加細分里程碑後，定位熱點在 `AfterCMMOldFetch`。
3. 進行 CMM 等價重排（先 `#TEMP1_CMM工作_AC`，再 join 明細）後，CMM 段落與總體時間有改善。
4. 發生過一次 push 阻擋（遠端超前），採 `pull --rebase` 後完成推送。
5. `stash pop` 後出現大量 `bin/obj` 衝突，先保留現場，再依使用者同意做安全整理。
6. 已清理：
   - 解掉 merge 衝突（僅建置產物）
   - 還原 `bin/obj` 產物變更
   - 刪除 `sql/_perf_run_*.log`
7. 持續進行下一輪 Dlytime 區段細拆量測。

## 重要效能觀察（本次）
- CMM 細分後確認：主要耗時集中於資料抓取（OldFetch），排序/最小序號段相對很小。
- CMM 重排版本（stage12）曾觀察到：
  - `AfterCMMOldFetch` 約由 ~19.8s 降到 ~16~17s。
  - `BeforeCommit` 約由 ~341s 降到 ~327s（單次/雙次皆有觀察）。
- 後續 summary trim 合併更新嘗試，結果波動較大，未形成穩定降秒結論。

## Git/環境處理紀錄
- 已依使用者指示「直接推」：`main` 已成功 push 到 `origin/main`。
- 推送過程處理：
  - non-fast-forward -> `pull --rebase` -> `push` 成功。
- 後續為保護現場：
  - 先盤點衝突清單與安全解法。
  - 經使用者同意後，只做低風險清理（建置產物與效能 log）。

## 目前狀態
- SQL 優化仍在進行中，已進入 Dlytime 子段定位。
- 非 SQL 專案（例如 `RAG`、`自動抓取匯率`）仍有既有修改，未被我清除。

## 使用者要求記錄（摘要）
- 「延續修改 / 繼續」
- 「先備份、先存到雲端」
- 「部署到資料庫、測試多久」
- 「直接推」
- 「保留目前工作樹並先整理衝突清單」
- 「只清理 bin/obj 與測速 log」
- 「把目前對話整理成 markdown 存檔」

## 檔案資訊
- 產出檔案：`G:\codex_pg\sql\對話紀錄_2026-04-07.md`
- 產出時間：2026-04-07（Asia/Taipei）
