# TODO

- [x] 將 `PUR2019P.pas` 查詢流程轉為 C#（已完成 `PURTM/PURTD` 單頭/單身查詢）。
- [x] 將 `PUR2019P.pas` 基本儲存流程（新增/修改/刪除/確認/取消確認/作廢）轉為 C#。
- [x] 補齊第一批進階檢核（`PURDEL` 防護、`ORDMENO.MPCHK` 同步、確認前可選 SP 檢核）。
- [x] 補齊第二批進階檢核（`PUPA1/PUPA2` 分段金額、MOQ）。
- [ ] 補齊 `PUR2019P.pas` 其餘欄位連動與特殊條件分支（含更多 UI 事件）。
- [ ] 將 `PUR2019AP*.pas` 管理功能拆分為服務層與畫面層。
- [ ] 盤點 `Utility.pas` 共用函式，分類為 DB、字串、報表、驗證四大模組。
- [ ] 補上資料庫安全設定（認證資訊改走安全祕密管理，不放明碼）。
- [ ] 建立 smoke test（啟動、查詢、明細開啟、確認/取消確認）與後續整合測試。
